# Plugin Architecture

The plugin architecture allows MusicInteractionFramework to remain open and extensible.

A plugin registers capabilities such as:

- interaction models
- metrics
- loaders
- renderers
- pipeline steps

The core remains small. The ecosystem grows through plugins.
